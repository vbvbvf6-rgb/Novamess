# GitHub Pages деплой отключён — github.io блокируется в России.
# Приложение деплоится целиком через Docker на Pxxl (см. pxxl.toml и deploy.yml).
# Этот файл оставлен пустым чтобы не сломать историю git.
