- [Object storage media offload](object-storage-media-offload.md) — media moved from inline Postgres base64 to S3-compatible storage via `offloadDataUrl()`; no free Postgres tier is "unlimited", so this is the real fix.
- [App rebranding](rebrand-nova.md) — App renamed from "Pulse"/"Aether" → "Nova" → "Aura" → back to "Nova"; internal localStorage/sessionStorage keys intentionally keep "pulse-"/"nova-" prefix to avoid logging out existing users.
- [PWA offline updates](pwa-update-flow.md) — SW no longer auto-calls skipWaiting(); instead waits for user to confirm via toast banner; hook in src/hooks/useServiceWorkerUpdate.ts + PwaUpdateBanner in App.tsx.
- [Drizzle-kit push interactive hang](drizzle-prod-push.md) — drizzle-kit push is interactive and hangs when prompted; use direct SQL for schema additions. For fresh DB: run SQL files via psql: `psql "$DATABASE_URL" -f lib/db/drizzle/0000_*.sql` etc.
- [Seed bulk upsert](seed-optimization.md) — gift catalog must use single bulk `INSERT ... ON CONFLICT (name) DO UPDATE`; requires `unique()` on `gift_items.name` in schema. Direct SQL used to add constraint.
- [Drizzle schema/DB drift](drizzle-generate-drift-danger.md) — `gift_items`/`gifts` tables exist in DB but were missing from schema.ts; `drizzle-kit generate` silently proposed `DROP TABLE ... CASCADE` for them. Always read a generated migration before applying it.
- [Render + pnpm monorepo deploy](render-pnpm-deploy.md) — esbuild externalizes `@aws-sdk/*` by default; bundle client-s3 instead (pure JS, no native deps) since Render doesn't reliably keep pnpm's symlinked node_modules between build/runtime.
- [SSE reconnect pattern](sse-reconnect.md) — both AppContext and ChatWindow SSE use exponential backoff (1s→2s→4s→…→30s max); reset retryCount on "open" event; ChatWindow SSE uses token auth via `_token` param.
- [Events spin/streak keys](events-features.md) — daily streak tracked in `nova-last-login` + `nova-streak` localStorage; spin tracked in `nova-spin-date`; quest sparks in `pulse-sparks` (legacy key kept for compatibility).
- [Persistent auth pattern](persistent-auth.md) — App.tsx useState initializer for userId auto-restores from getSavedAccounts()[0].token on fresh window; handleLogin already saves token to pulse-accounts localStorage; pg module at node_modules/.pnpm/pg@8.20.0/node_modules/pg.
- [Admin account](admin-account.md) — Admin user is id=1, username=creater_messenger; is_admin=true set in DB; password reset via direct SQL UPDATE using bcrypt hash. ADMIN_USER_IDS=[4] in platform-events.ts but isAdminUser() also checks DB is_admin column.
- [Mobile/orientation bug patterns](mobile-bug-patterns.md) — landscape: cap textarea max-h to window.innerHeight/4; modals need max-h-[90dvh] overflow-y-auto; Stories safe-area: use paddingLeft/Right env() not just paddingTop; recall/action buttons must not be opacity-0 (no hover on touch); autofocus only on hover-capable devices via matchMedia.
- [TypeScript TS6305 pre-existing](ts6305-ignore.md) — TS6305 errors ("Output file has not been built") on api-client-react/dist are pre-existing; don't fix them, Vite uses source directly. Only fix TS7006/TS2345/TS2344 real errors.
- [Auth routes for public pages](auth-public-routes.md) — /privacy and /terms must be added to AuthPages Switch in App.tsx, not just MainApp; otherwise unauthenticated users get 404 when clicking those links from login/register.
- [Private group security](private-group-security.md) — Discover route filters private groups (only shows channels OR is_public=true OR user is member); join route rejects private groups with 403. Both in artifacts/api-server/src/routes/chats.ts.
- [Prime+ Lounge visibility](prime-lounge.md) — After Prime+ subscribe, must invalidate both /api/users/me AND ["/api/chats"] query keys; otherwise the Lounge chat won't appear until page refresh.
- [Settings dead code](settings-dead-code.md) — FeaturesSection, aiAvatarGenerating state, handleGenerateAIAvatar, emojiSize state all removed as dead code. Wand2 removed from lucide imports.
- [WebRTC call signaling](webrtc-call-signaling.md) — pre-offer in startCall was the bug; peer-joined handler creates the offer; identity guard in onconnectionstatechange; relay fallback via createPeerRef.
- [Call minimize/media fixes](call-minimize-fixes.md) — Hidden off-screen `<video ref={remoteVideoRef}>` and `<audio ref={remoteAudioRef}>` kept in DOM inside the minimized pill JSX so refs stay valid and srcObject survives minimize/expand. getUserMedia uses 3-tier fallback; visibilitychange resumes audio when tab visible.
- [Contact requests feature](contact-requests-feature.md) — contact_requests table added via startup SQL; routes in artifacts/api-server/src/routes/contact-requests.ts; SSE events "contact-request"/"contact-request-accepted"; badge uses "pulse:contact-requests-resolved" custom event to re-fetch count on accept/decline.
- [Admin DM block removed](admin-dm-block.md) — Previous code blocked all DMs to is_admin=true users; removed so creater_messenger is reachable by everyone.
- [JWT TTL and session persistence](jwt-token-ttl.md) — TOKEN_TTL set to "90d"; JWT_SECRET falls back to SESSION_SECRET if not explicitly set (add JWT_SECRET to Replit Secrets for prod). 401 handler only clears sessionStorage, NOT localStorage, so tokens survive restarts.
- [AI bot username](ai-bot-rename.md) — AI bot username changed from deepseek_ai → nova_ai; seed.ts updated; admin.ts broadcast query updated; ai.ts BOT_USERNAME updated; messages.ts system prompt updated to "Nova AI".
- [Gifts removed](gifts-removed.md) — Gifts nav item removed from Sidebar; /gifts route removed from App.tsx; Gift import removed from Register.tsx; backend gifts route still exists but is not linked from UI navigation.
- [Rejected posts hidden from all](rejected-posts-hidden.md) — posts.ts GET /posts filter changed: rejected posts hidden from everyone except admins (removed author exception); BlockedPostCard still shown from author's own view via moderationStatus check in PostCard.
- [Admin deletion pre-cleanup](admin-deletion-fix.md) — user_reports, post_reports, contact_requests DELETE runs OUTSIDE the transaction with .catch(() => {}) so missing tables don't abort user deletion; CREATE TABLE IF NOT EXISTS for these tables added to startup SQL in admin.ts.
- [Post reports feature](post-reports.md) — Backend: POST /posts/:postId/report in posts.ts; CREATE TABLE IF NOT EXISTS post_reports in admin.ts startup; Frontend: Flag icon + report dialog in Feed.tsx PostCard (showReportPost/reportReason/reportDetails state).
- [Chat folders renamed](chat-folders.md) — System folders: "Личные"→"Контакты", "Группы"→"Каналы/Группы"; Contacts tab before Groups; FolderKey union type now includes "direct"; redundant cast removed from filter at line ~443.
- [Account deletion column name](account-deletion-column.md) — user_reports table uses `target_id` (not `reported_id`); DELETE in users.ts must reference `target_id`.
- [Body size limits](body-size-limits.md) — messages: 500MB, upload: 100MB, stories: 50MB, users/me: 50MB, support: 25MB. Large limits needed for base64-encoded file transfers. Limits apply before auth middleware (DoS note).
- [Profile action buttons mobile](profile-action-buttons.md) — UserProfile.tsx action buttons use `flex-nowrap overflow-x-auto scrollbar-none` so they scroll horizontally on small screens instead of wrapping/squishing.
- [No-card object storage options](no-card-object-storage.md) — Cloudflare R2 and Backblaze B2 both demand a card/billing verification despite advertised free tiers; Supabase Storage is the only S3-compatible option confirmed card-free (but only 1GB free).
- [Client-side video compression](client-video-compression.md) — no external object storage in this app, so raw video uploads bloated Postgres; added canvas+MediaRecorder re-encode before upload with graceful fallback to raw file if unsupported/not smaller.
- [Account moderation system](account-moderation-system.md) — bans now have reason+optional expiry (ban_expires_at null=permanent); deletions require a reason stored in deleted_accounts, surfaced to the user at next login attempt.
- [Maintenance enforcement](maintenance-enforcement.md) — maintenance mode used to be cosmetic-only (no backend block); now enforced via /api middleware in app.ts checking app_settings 'maintenance' key, admins exempt.
